kind: pipeline
type: docker
name: default

steps:
# - name: docker-image
#   image: ubuntu
#   steps:
- name: build
  image: plugins/docker
  volumes:
  - name: dockersock
    path: /var/run/docker.sock
  commands:
  - sudo docker build -f Dockerfile -t first:latest . 
#   commands:
#   - ls
#   - sudo docker build -t my_first_image .
  #- systemctl start docker
  #- systemctl status docker
  #- echo image created
