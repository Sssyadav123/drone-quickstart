 ---
# kind: pipeline
# type: docker
# name: default

# # remove this section if your CPU is amd64
# #platform:
#  # os: linux
#  # arch: arm64

# steps:
# - name: hello
#   image: busybox
#   commands:
#   - echo This is Demo of Drone ci

# trigger:
#   branch:
#   - main
  
kind: pipeline
type: docker
name: default

steps:
- name: test
  image: golang
  commands:
  - go build
  - go test
